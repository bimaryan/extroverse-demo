class Distributor{
  int? _id;
  String? _nama;
  String? _alamat;
  int? _phonenumber;

  //konstruktor
  Distributor(this._id, this._nama, this._alamat, this._phonenumber);

  //getter dan setter untuk nama
  String get nama => _nama;
  set nama(String nama) => _nama = nama;

  //getter dan setter untuk email
  
}